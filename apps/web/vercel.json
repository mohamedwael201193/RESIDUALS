{
  "framework": "vite",
  "buildCommand": "cd ../.. && npm ci && npm run build -w @residuals/shared && npm run build -w @residuals/web",
  "outputDirectory": "dist",
  "rewrites": [{ "source": "/(.*)", "destination": "/index.html" }]
}
